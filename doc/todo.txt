1.The Module of new own model format and The tool application transfer the new model format to the standardised model
format (COLLADA).
2.Privatize some template code and move them from inl files to cpp files.
3.Perfect binding priority and Add register feature of binding priority.
4.Perfect light system and Add some necessary light types.
5.Post render for entire screen after render.
6.Change render flow for every scene as: pre_z -> render -> post render.